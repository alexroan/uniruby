require 'test_helper'

class ClaimTest < ActiveSupport::TestCase
  #test "invalid with empty attributes" do
    #claim = Claim.new
    #assert !claim.valid?
    #assert claim.errors[:claim_date].any?
    #assert claim.errors[:value].any?
    #assert claim.errors[:claim_type].any?
    #assert claim.errors[:person_pk].any?
  #end
end
